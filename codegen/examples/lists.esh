num[] myList;
string[Player] myMap;

func test() {
    num hello = 15;
    hello = myList[4 + 2];

    myList[10] = hello;
    Player myPlayer = Player {
        uuid = "hello";
        hp = 200;
    };
    myMap[myPlayer] = "wow";
    hello = 100000;

    myPlayer.uuid = myMap[myPlayer];
}

struct Player {
    string uuid;
    num hp;
}