
domain yo {
    func hello(string hell, num add) -> string {
        num wowvar;
        wowvar = ((2 + 1) * 4) + (3 + 6);
        if (wowvar == 4 || (wowvar == 5 && add < 3)) {
            add = 123;
            num testvar = 5;
            testvar = testvar + 999;
            return "aa" * testvar;
        }
        num testvar = 17;
        testvar = testvar + 888;
        wowvar = wowvar + add;
        return "g" * wowvar;
    }
    domain hey {
        func blablabla(string hell, string add) {
            string wowvar = "wow";
            wowvar = wowvar + hell;
            wowvar = hello("hey" + hello("whaaat" + "hey", 15), 10) + "hello";
        }
    }
    func wtf(Player pl) -> num {
        Player hey = Player {
            uuid = "hello";
            hp = 0;
            inv = Inventory {
                item_amount = 0;
            };
        };
        return hey.inv.item_amount;
    }
    struct Player {
        string uuid;
        num hp;
        Inventory inv;
    }
    struct Inventory {
        num item_amount;
    }
}

func randomfunc(string hell, string add) {
    string wowvar = "wow";
    wowvar = wowvar + hell;
    yo.hey.blablabla("wowwww", "hey");
    wowvar = yo.hello("hey", 5);
}
